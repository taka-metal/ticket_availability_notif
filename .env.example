# GitHub Secrets として設定する環境変数
# このファイルはテンプレートです。実際の値は GitHub Secrets に設定してください。
# ローカルテスト用に .env ファイルをコピーして使用できますが、
# .env は絶対にリポジトリにコミットしないでください。

# 監視するぴあのチケットURL
TICKET_URL=https://ticket.pia.jp/pia/event/XXXXXXXXXX.do

# 送信元Gmailアドレス
GMAIL_USER=your-email@gmail.com

# Gmailアプリパスワード（Googleアカウント → セキュリティ → アプリパスワード で発行）
# 通常のGmailパスワードではなく、アプリパスワードを使用すること
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# 通知先メールアドレス（送信元と同じでも可）
NOTIFY_TO=notify-destination@example.com
